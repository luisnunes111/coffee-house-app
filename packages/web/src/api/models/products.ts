/**
 * Get applications List
 */
const getList = (): Utils.ApiResponse<Models.IProductsList> | null => {
	try {
		//get results
	} catch (error) {}
	return null;
};

export default {
	getAll: getList,
};
